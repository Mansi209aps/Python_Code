class Solution:
    def countDigits(self, n):
        count=0
        temp=0
        while n!=0:
            temp = n%10
            n = int(n/10)
            count = count+1
        return count


cnt = int(math.log10(n) + 1)
